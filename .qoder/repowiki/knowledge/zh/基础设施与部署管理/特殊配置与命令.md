- **本地开发启动**：运行 `python scripts/running/run_dev.py` 可自动触发迁移并启动带热重载的服务。
- **Docker 部署**：在项目根目录执行 `docker compose -f docker/docker-compose.yml up -d` 即可拉起包含数据库和应用的完整环境。
- **手动迁移**：可通过 `bash scripts/testing/run_migrations.sh` 独立执行数据库结构更新。