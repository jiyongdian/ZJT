- 启动脚本（如 `run_dev.py` 和 `run_prod.py`）均实现信号处理机制，确保父进程退出时能自动清理所有子进程（Web 服务和调度器）。
- 数据库连接配置在 Alembic 环境和 Docker 入口脚本中均通过环境变量或 YAML 配置文件动态注入，避免硬编码。
- 跨平台启动逻辑根据 `platform.system()` 自动切换 Web 服务器实现，并为 macOS 设置 `OBJC_DISABLE_INITIALIZE_FORK_SAFETY` 以规避崩溃。