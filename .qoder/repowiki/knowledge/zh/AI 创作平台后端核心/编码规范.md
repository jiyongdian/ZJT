- 所有 API 路由均通过 `Authorization` 请求头提取 Token，并调用 `UserTokensModel` 或 `AuthService` 进行身份校验。
- 数据库写操作优先使用 `model/database.py` 中的 `transaction()` 上下文管理器以确保事务一致性。
- 耗时或同步的内部服务调用（如算力检查）在异步路由中通过 `asyncio.to_thread` 封装执行。