- 每个业务表对应一个独立 Python 文件，内部定义一个数据实体类（如 User）和一个以 Model 结尾的操作类（如 UsersModel）。
- 所有数据库操作方法均声明为 @staticmethod，并通过调用 database.py 中的全局工具函数执行 SQL。
- 在模型文件末尾通过 CREATE_TABLE_SQL 常量维护建表语句，作为 Schema 定义的参考基准。
- 复杂更新或插入操作优先使用参数化查询（%s 占位符），并在操作类内部处理日志记录与异常捕获。