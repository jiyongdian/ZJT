- **API层**：`api/user.py` 基于 FastAPI 定义用户偏好路由，区分社区版（返回模拟数据）与商业版逻辑。
- **服务层**：`services/checkin_service.py` 封装签到核心业务，包括连续天数计算、动态奖励配置解析及算力发放；`services/edition_auth_service.py` 实现异步授权心跳检测与异常上报。
- **依赖方向**：API 路由与服务类均依赖 `model` 层进行数据持久化，并通过 `config` 模块获取动态配置与版本策略（`IS_COMMUNITY_EDITION`）。