- 时间精度统一采用微秒（microseconds）作为内部存储和计算单位，通过 `seconds_to_microseconds` 等工具函数进行转换。
- 所有资源标识符（如轨道 ID、素材 ID）均通过 `_generate_id` 方法生成，格式为“前缀 + UUID 十六进制大写截取”。
- 媒体处理采用“尝试 FFprobe 获取 -> 失败则根据配置回退或抛出异常”的健壮性模式，确保时长获取的准确性。