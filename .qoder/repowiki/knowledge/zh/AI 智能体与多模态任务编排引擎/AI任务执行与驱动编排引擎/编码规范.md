- 所有视觉驱动必须继承 `BaseVideoDriver` 并实现 `submit_task`、`check_status` 等抽象方法。
- 驱动实例化统一通过工厂类（`VideoDriverFactory` / `PipelineDriverFactory`）完成，禁止在业务代码中直接 `new` 驱动对象。
- 异步任务提交需遵循统一的槽位获取与指数退避重试机制，由 `async_task_submission` 模块集中管理。