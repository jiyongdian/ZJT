- 所有技能和 SOP 文件均采用 Markdown 格式，并在文件头部使用 `---` 包裹的 YAML Front Matter 定义 `name`、`description` 和 `allowed-tools` 等元数据。
- SOP 配置文件 `sops_config.json` 用于映射 SOP 名称与其允许使用的额外工具列表，实现权限与流程的解耦。
- 技能描述文件（SKILL.md）中普遍包含“提示词保护规则”、“核心执行原则”及“禁止假动作”等强制性行为约束章节。