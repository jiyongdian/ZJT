- 所有 LLM 客户端继承自 `base_llm_client.BaseLLMClient` 并实现统一调用接口
- 多模态任务驱动遵循 `BaseVideoDriver` 或 `BaseAsyncDriver` 抽象基类规范
- 智能体技能文件采用 Markdown Front Matter 格式定义元数据与 SOP 索引
- 工具执行器通过 `tool_map` 字典映射工具名称到具体函数，支持动态注册