- 采用函数式编程风格，通过闭包或全局函数暴露 API，避免使用 ES6 Class 封装模块。
- DOM 操作直接通过 `document.querySelector` 和 `innerHTML` 进行，未使用虚拟 DOM 或前端框架组件化开发。
- 状态变更后立即调用对应的 `render...` 函数进行手动视图同步，遵循命令式更新模式。
- 使用 `data-node-id` 等 data 属性在 DOM 元素与状态对象之间建立双向映射关系。