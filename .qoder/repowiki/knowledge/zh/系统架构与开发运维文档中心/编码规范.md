- 异步非阻塞原则：所有涉及网络请求或 I/O 的操作必须使用 `aiohttp` 和 `aiofiles`，严禁在异步上下文中使用同步的 `requests` 库。
- 配置集中化管理：新增任务类型或驱动配置需在 `config/unified_config.py` 中通过 `UnifiedTaskConfig` 声明式定义，并自动注册至全局注册表。
- 跨平台路径处理：文件系统操作统一使用 `pathlib.Path` 并确保 UTF-8 编码，以兼容 Windows/macOS/Linux 的路径差异。