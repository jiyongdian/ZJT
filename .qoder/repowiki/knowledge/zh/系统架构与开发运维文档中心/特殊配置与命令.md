- **依赖同步**：推荐使用 `uv sync` 或 `conda` 环境初始化。
- **服务启动**：Windows 下通过 `scripts/launchers/start_windows.py` 自动编排 MySQL 初始化、迁移及服务监控；Linux/macOS 使用 `scripts/running/run_prod.py`。
- **打包构建**：执行 `scripts/build/build.bat` 生成带系统托盘功能的 Windows 可执行文件。